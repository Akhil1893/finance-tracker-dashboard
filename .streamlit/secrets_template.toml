# Template: copy to .streamlit/secrets.toml (do NOT commit your real keys)

[secrets]
# Provider string, e.g. "openai" or "anthropic"
llm_provider = "openai"
# Your hosted LLM API key
llm_api_key = "YOUR_API_KEY_HERE"
# Optional model name
llm_model = "gpt-4o-mini"
